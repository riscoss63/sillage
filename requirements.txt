# The assistant itself needs only the first three (or: pip install -e .)
numpy>=1.24
torch>=2.0
transformers>=4.51

# Reproducing the papers (REPRODUCE.md) additionally needs
scipy>=1.10
matplotlib>=3.7
